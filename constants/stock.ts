// Shared across the Home dashboard's low-stock rail (useDealerSummary) and
// catalog product cards' low-stock badge — kept in one place so both stay in
// sync. Not a backend concept; purely a client-side display threshold.
export const LOW_STOCK_THRESHOLD = 10;
